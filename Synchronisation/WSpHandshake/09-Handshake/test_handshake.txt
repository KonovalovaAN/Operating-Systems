test handshake
к проекту handshake добавить библиотеку неявным методом
1) имя библиотеки Konovalova.dll
2) экспортировать две функции: add(x, y) аргуметны и возвращаемые значения float | x+y
	multiply(x, y) аргуметны и возвращаемые значения float | x*y
3) клиент в строке request к серверу должен задавать две специальные строка
	а) add 2.5 0.11
	б) mult 2.5 0.11
4) серверный поток принимает запрос в буфере 
расшифровывает код операции вызыввает экспортируемые функции 
соответствующие операциям возвращает результат главному потоку